extends base
block header 
    h1 Edit #{video.title}
block content
   h3 Change Title of  "#{pageTitle}"
   form(method="post")
        input(type="text" value=video.title name="title")
        input(type="submit" value="submit")