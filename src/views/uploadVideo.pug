extends base
block content
   h3 Upload  
   if errorMessage 
        span=errorMessage
   form(method="POST")
        input(type="text", name="title", required, placeholder="Title", maxlength="14" )
        input(type="text", name="description", required, placeholder="Description",maxlength="90")
        input(type="text", name="hashtags", required, placeholder="Hashtags")
        input(type="submit", value="submit")